# 💎 Sapphire Collective: DeepAudit API Codex

> *Auto-generated by DeepAudit v0.3.1 Engine*

---

## 📦 Module: `deepaudit\cli.py`

### `def display_header()`

Render the CLI header.

### `def run_audit(file_path)`

*Warning: No docstring provided.*

### `def main()`

*Warning: No docstring provided.*

## 📦 Module: `deepaudit\engine\parser.py`

### Class: `CodeParser`

The DeepAudit Tree-sitter parsing engine.
Transforms raw Python source code into an Abstract Syntax Tree (AST)
for the Scanner Registry to analyze.

### `def __init__(self, file_path)`

*Warning: No docstring provided.*

### `def parse(self)`

Reads the target file and parses it into an AST.

Returns:
    tree_sitter.Node: The root node of the generated AST.

## 📦 Module: `deepaudit\scanners\dependency.py`

### `def scan(ast_node, raw_code, file_path)`

Scans for known hallucinated or dangerous AI dependencies.

## 📦 Module: `deepaudit\scanners\scanners.py`

### Class: `ScannerRegistry`

The central execution hub for DeepAudit's security modules.

This registry dynamically discovers and loads all standalone scanners 
(e.g., dependency, secret, static) residing in the scanners directory. 
It ensures that new vulnerability checks can be added to the flagship 
without modifying the core execution loop.

### `def __init__(self)`

Initializes the registry and maps all available scanners.

### `def run_all(self, ast_node, raw_code, file_path)`

Pipes the parsed AST through every registered security scanner.

Args:
    ast_node (Any): The root Tree-sitter node of the parsed file.
    raw_code (str): The raw source code as a string.
    file_path (str): The absolute path to the file being audited.

Returns:
    List[Dict[str, Any]]: An aggregated list of all vulnerabilities found.

## 📦 Module: `deepaudit\scanners\secret.py`

### `def scan(ast_node, raw_code, file_path)`

Scans for high-entropy strings indicating hallucinated or leaked secrets.

## 📦 Module: `deepaudit\scanners\static.py`

### `def scan(ast_node, raw_code, file_path)`

Performs static analysis to catch dangerous execution functions.

## 📦 Module: `deepaudit\utils\crawler.py`

### Class: `SourceCrawler`

No documentation provided.

### `def __init__(self, root_dir, exclude_init)`

*Warning: No docstring provided.*

### `def get_python_files(self)`

*Warning: No docstring provided.*

## 📦 Module: `deepaudit\utils\logger.py`

### `def get_logger(name)`

Returns a Rich-integrated logger.
Ensures backend modules don't break the UI.

